import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'MediSphere - Connected Healthcare Ecosystem',
  description: 'A secure, cloud-based Hospital Management System that streamlines clinical operations, improves administrative efficiency, and enhances the patient experience.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
